fn main() {
    let n = 42; // i32

    let x = 2.0; // f64
    let y: f32 = 3.0; // f32

    let c = 'z'; // char

    // variety of types into one compound type
    // fixed length
    let tup: (i32, f64, u8) = (500, 6.4, 1);

    // same type
    // fixed length
    let a = [3; 5]; //[3, 3, 3, 3, 3]
}
